{
  "name": "sledopyt-kasoda",
  "version": "0.3.0",
  "private": true,
  "description": "Альтернативный Следопыт (Kasoda) — homebrew Ranger class module for Foundry VTT (dnd5e).",
  "type": "module",
  "scripts": {
    "validate": "node tools/validate.mjs",
    "pack": "node tools/pack.mjs",
    "build": "npm run validate && npm run pack",
    "generate": "python3 tools/build.py && python3 tools/build_subclasses.py",
    "zip": "cd \"$(node -p \"process.cwd()\")\" && zip -r module.zip module.json README.md LICENSE packs -x '*/LOCK'"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.1.0"
  }
}
